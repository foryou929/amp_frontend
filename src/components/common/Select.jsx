const Select = ({ ...rest }) => {
    return (
        <div {...rest}>
            <select className={`w-full p-2 border border-[#DEE2E6] bg-[#F8F9FA]`}></select>
        </div>
    )
}

export default Select;